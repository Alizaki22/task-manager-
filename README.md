# Task Manager

A simple fullstack task management application built with Node.js, Express, MongoDB, and vanilla HTML/CSS/JavaScript.






































- `DELETE /api/tasks/:id` - Delete a task- `POST /api/tasks` - Create a new task- `GET /api/tasks` - Fetch all tasks## API Endpoints4. Open `http://localhost:5000` in your browser```npm start```bash3. Start the server:2. Set up MongoDB connection in `db.js````npm install```bash1. Install dependencies:## How to Run- **Database:** MongoDB- **Backend:** Node.js, Express- **Frontend:** HTML, CSS, JavaScript## Tech Stack- RESTful API backend- Responsive design for mobile and desktop- Delete tasks- View all tasks in a dynamic list- Create new tasks with title and description## Features
## Features

- Create new tasks with title and description
- View all tasks in a dynamic list
- Delete tasks
- Responsive design for mobile and desktop
- RESTful API backend

## Tech Stack

- **Frontend:** HTML, CSS, JavaScript
- **Backend:** Node.js, Express
- **Database:** MongoDB

## How to Run

1. Install dependencies:



- `DELETE /api/tasks/:id` - Delete a task
- `POST /api/tasks` - Create a new task
- `GET /api/tasks` - Fetch all tasks

## API Endpoints

4. Open `http://localhost:5000` in your browser

```
npm start
```bash

3. Start the server:

2. Set up MongoDB connection in `db.js`

```
npm install```bash